# === DATABASE CONNECTION ===
# For local SQLite:
DATABASE_URL=sqlite:///./countries.db

# For PostgreSQL (example):
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/countries_db


# === API SOURCES ===
# Fetch all countries' info (name, capital, region, flag, etc.)
COUNTRIES_API=https://restcountries.com/v2/all

# Fetch real-time exchange rates
EXCHANGE_RATES_API=https://open.er-api.com/v6/latest/USD


# === APP SETTINGS ===
# Optional app environment flag
APP_ENV=development

# Optional port override (used if deploying)
PORT=8000
